pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Scan') {
      steps {
        withSonarQubeEnv(installationName: 'Bilgeadam_Sonarqube') { 
          sh 'clean verify sonar:sonar'
        }
      }
    }
  }
}
